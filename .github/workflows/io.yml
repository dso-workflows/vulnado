name: DevSecOps

on:
  workflow_dispatch

jobs:
  devsecops:
    name: DevSecOps
    uses: io-workflows/io-workflows/.github/workflows/devsecops.yml@main
    with:
      verbose-option: 'SilentlyContinue'
      project-language: 'Java'
#       project-name: 'MyProject'
#       branch-name: 'MyBranch'
    secrets: inherit
